<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.smartcity.petsservices">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_pets"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_pets_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetsServices"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.activity.EditProfileActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.PetsServices.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
       <activity android:name=".ui.activity.LoginActivity"
           android:label="@string/app_name"
           android:theme="@style/Theme.PetsServices.NoActionBar" >
       </activity>
    </application>

</manifest>